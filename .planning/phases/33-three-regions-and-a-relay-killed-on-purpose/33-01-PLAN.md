---
phase: 33-three-regions-and-a-relay-killed-on-purpose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cloudflare/src/hosted-object.ts
  - packages/cloudflare/src/hosted-identity.test.ts
  - packages/cloudflare/src/hosted-placement.test.ts
  - vitest.config.ts
autonomous: true
requirements: [HOST-06]

must_haves:
  truths:
    - "An operator reading source can see that the `eu` object and the `sam` object are placed by two different mechanisms, without opening a second file"
    - "A placement value outside the closed set is refused by name before anything is sited"
    - "The `us` path is byte-unchanged, so the already-created object keeps its identity"
  artifacts:
    - path: "packages/cloudflare/src/hosted-object.ts"
      provides: "the closed jurisdiction union, the closed location-hint set, their named refusals, and the two non-symmetric placement helpers"
      contains: "euJurisdictionOf"
    - path: "packages/cloudflare/src/hosted-placement.test.ts"
      provides: "spy-namespace proof that the two helpers reach two different platform calls, and that neither accepts an undeclared value"
  key_links:
    - from: "packages/cloudflare/src/hosted-object.ts"
      to: "namespace.jurisdiction"
      via: "euJurisdictionOf, which returns a narrowed namespace and never a stub"
      pattern: "jurisdiction\\(HOSTED_JURISDICTION\\.eu\\)"
    - from: "packages/cloudflare/src/hosted-object.ts"
      to: "namespace.get"
      via: "stubFor's third argument, passed through untouched"
      pattern: "namespace\\.get\\("
---

<objective>
Give this repository a vocabulary for **where an object is placed** that makes the difference
between a binding jurisdiction and a non-binding hint visible in source, and refuses an
undeclared placement value by name.

## Phase Goal

**As a** fabric operator, **I want** `bootstrap-us`, `bootstrap-eu` and `bootstrap-sam` to
exist as three identities in three objects under the closed name set, with nothing anywhere
claiming where any of them actually runs, **so that** a scheduled, repeated drill measures what
a region's loss costs instead of my discovering it during the run.

*(The three slots are quoted from `.planning/ROADMAP.md:2417` verbatim. The template's
`**I want to**` is not forced onto the owner's wording — the sentence says "I want … to
exist", and rewriting it to fit a bolding convention would edit the goal to fit the form.)*

**This plan's operator-visible slice:** after it, an operator can read one file and see that the
two placed regions are placed by two different platform mechanisms, and can watch an undeclared
placement value refused.

**Every task here is `agent-now`.** Nothing in this plan creates a Cloudflare resource, deploys,
or reaches the network.

Purpose: `HOST-06` (`.planning/REQUIREMENTS.md:2193`) states that the three regions *"must not
be created by one uniform code path that hides the difference"*. A single
`createRegion(name, placement)` taking a union and branching internally **is** that failure.
This plan supplies two separately-named helpers instead, each carrying its own literal, with no
shared parameter either of them branches on.

Output: an extended `hosted-object.ts`, a new unit spec, updated `vitest.config.ts` counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-three-regions-and-a-relay-killed-on-purpose/33-CONTEXT.md
@CLAUDE.md

Measured 2026-09-13 against this tree, and recorded here because it exists nowhere else yet —
route each of these three readings into a docblock as the task actions say, or the next agent
re-derives them or assumes the opposite:

1. **A Durable Object binding cannot express placement in `wrangler.jsonc`.**
   `node_modules/wrangler/config-schema.json` → `definitions.DurableObjectBindings` declares
   exactly `name`, `class_name`, `script_name`, `environment` with `additionalProperties:
   false`. The only `jurisdiction` keys anywhere in that schema belong to `r2_buckets` and to
   `ContainerApp.constraints`. So placement is expressed **in code** and nowhere else.
2. **The installed workerd declares a four-member jurisdiction union.** Read out of the
   `node_modules/@cloudflare/workerd-darwin-arm64/bin/workerd` binary's own bundled type text:
   `type DurableObjectJurisdiction = "eu" | "fedramp" | "fedramp-high" | "us"`. `HOST-06`'s
   ledger row names three. The union declared in source is the platform's four, with the
   discrepancy recorded — an executor who declares three contradicts the platform and one who
   declares four silently looks like they contradicted the ledger.
3. **A location hint is not validated by the platform at all.** The same local runtime accepted
   `{ locationHint: 'notareal' }` and returned a live stub. A closed set in source is therefore
   the only thing in existence that can refuse a mistyped hint.

`hosted-object.ts:104-122` is the one call site that may obtain a stub, `HOST-08` is `[x]`
against it, and `packages/node/src/hosted-tier-deploy.node.test.ts:195-238` is the guard that
holds it — **exactly one `idFromName(` in the repository's one permitted file**. This plan keeps
that count at one and the `.get(` count at one. The difference between the three placements is
carried by *what is handed to* `stubFor`, never by a branch inside it.
</context>

<interfaces>
<!-- The contracts downstream plans (33-02, 33-03) implement against. Extracted from the tree
     2026-09-13. Executor: use these directly; no exploration needed. -->

From `packages/cloudflare/src/hosted-object.ts` — today:

  export const HOSTED_OBJECT_NAME = { us: 'bootstrap-us', eu: 'bootstrap-eu', sam: 'bootstrap-sam' } as const
  export type HostedObjectName = (typeof HOSTED_OBJECT_NAME)[keyof typeof HOSTED_OBJECT_NAME]
  export const HOSTED_OBJECT_NAMES: readonly HostedObjectName[]
  export interface HostedObjectNamespace<Stub> { idFromName: (name: string) => unknown; get: (id: unknown) => Stub }
  export class UnknownHostedObjectNameError extends Error
  export function stubFor<Stub>(namespace: HostedObjectNamespace<Stub>, name: HostedObjectName): Stub

From `packages/cloudflare/src/worker.ts:1386-1392` — today, and it does not move in this phase:

  const SERVED_BY: HostedObjectName = 'bootstrap-us'
  export default { async fetch(request, env) { return stubFor(env.BOOTSTRAP, SERVED_BY).fetch(request) } }

From `packages/cloudflare/src/hosted-identity.test.ts:194-214` — the spy shape to reuse and to
keep compiling:

  readonly namespace: Parameters<typeof stubFor<string>>[0]
  // built by spyNamespace(), whose object literal implements `idFromName` and `get`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Declare the placement vocabulary and the two non-symmetric helpers</name>
  <files>packages/cloudflare/src/hosted-object.ts, packages/cloudflare/src/hosted-identity.test.ts</files>
  <read_first>
    - `packages/cloudflare/src/hosted-object.ts` — all 177 lines. The header's three
      irreversibilities and `stubFor`'s docblock are the argument this task extends.
    - `packages/cloudflare/src/durable-object-storage.d.ts` — the "declare the platform surface
      as narrowly as it is used, so a fixture can implement it COMPLETELY" discipline that
      `HostedObjectNamespace` already follows and that the new members must follow.
    - `packages/cloudflare/src/do-datastore.ts` — `REFUSED_NAMESPACE`, the `as const` +
      derived-union + derived-array shape every closed set in this package copies.
    - `packages/cloudflare/src/hosted-identity.test.ts:190-230` — the spy namespace whose type
      is `Parameters<typeof stubFor<string>>[0]`, so any member added to
      `HostedObjectNamespace` ripples into this file and must be supplied there.
  </read_first>
  <action>
`agent-now`. No network, no resource, no deploy.

Extend `packages/cloudflare/src/hosted-object.ts` only. Do not touch `worker.ts`.

**1. The jurisdiction union, declared as the platform declares it.**
Add `export type DurableObjectJurisdiction = 'eu' | 'fedramp' | 'fedramp-high' | 'us'`. Its
docblock records, dated 2026-09-13: the four members were read out of the installed
`node_modules/@cloudflare/workerd-darwin-arm64/bin/workerd` binary's own bundled type text, not
out of documentation; `HOST-06`'s ledger row at `.planning/REQUIREMENTS.md:2193` names three of
them; the fabric uses exactly one. State that a wider union here would let a typo compile, and
that this union is what refuses an undeclared value at the earliest point a creation call can
exist.

**2. The fabric's own jurisdiction constant.**
Add `export const HOSTED_JURISDICTION = { eu: 'eu' } as const satisfies Record<string,
DurableObjectJurisdiction>`. One member, because one region is placed by jurisdiction. Its
docblock states that `bootstrap-us` is deliberately **absent** from this object and why — see
point 5.

**3. The location-hint set, closed in source because the platform closes nothing.**
Add `export const HOSTED_LOCATION_HINT = { sam: 'sam' } as const`, `export type
HostedLocationHint = (typeof HOSTED_LOCATION_HINT)[keyof typeof HOSTED_LOCATION_HINT]`, and
`export const HOSTED_LOCATION_HINTS: readonly HostedLocationHint[]` derived with
`Object.values`, following `HOSTED_OBJECT_NAMES` exactly. Its docblock records the 2026-09-13
measurement: a local `workerd` accepted `{ locationHint: 'notareal' }` and returned a live
stub, so the platform validates nothing and this set is the only refusal that exists.

**4. Two named refusals, on `UnknownHostedObjectNameError`'s shape.**
Add `export class UnsupportedJurisdictionError extends Error` and `export class
UnknownLocationHintError extends Error`. Each message names the offending value in quotes, the
count of declared members, and the declared members joined by `, ` — copy
`UnknownHostedObjectNameError`'s message construction, including setting `this.name`.

**5. Two placement helpers, separately named, neither taking a placement union.**
  - `export function euJurisdictionOf<Stub>(namespace: HostedObjectNamespace<Stub>):
    HostedObjectNamespace<Stub>` — returns `namespace.jurisdiction(HOSTED_JURISDICTION.eu)`.
    It returns a **namespace**, never a stub, so it sites nothing. Before calling, it throws
    `UnsupportedJurisdictionError` naming `HOSTED_JURISDICTION.eu` when
    `typeof namespace.jurisdiction !== 'function'` — a runtime that does not offer the method
    must be refused by name rather than crash on `undefined`.
  - `export function samLocationHint(): HostedObjectGetOptions` — returns
    `{ locationHint: HOSTED_LOCATION_HINT.sam }`, and throws `UnknownLocationHintError` if the
    value it is about to return is not in `HOSTED_LOCATION_HINTS`. Takes **no parameter**.
  - Write no third helper for `us`. Its docblock — on `euJurisdictionOf` — states the reason in
    these terms: `namespace.jurisdiction('eu').idFromName(n)` derives a **different object ID**
    than `namespace.idFromName(n)`, the `bootstrap-us` object was created through the plain
    namespace and has carried real traffic since 2026-08-27, so wrapping the `us` path in any
    jurisdiction now would address a new object and orphan the live identity. Absence of a
    helper is the placement.

**6. `stubFor` gains a pass-through third argument, and no branch.**
Add `export interface HostedObjectGetOptions { readonly locationHint: HostedLocationHint }`.
Widen `HostedObjectNamespace<Stub>` to `{ idFromName: (name: string) => unknown; get: (id:
unknown, options?: HostedObjectGetOptions) => Stub; jurisdiction?: (jurisdiction:
DurableObjectJurisdiction) => HostedObjectNamespace<Stub> }` — `jurisdiction` optional, so a
fixture can still implement the interface completely. Change `stubFor`'s signature to
`(namespace, name, options?: HostedObjectGetOptions)` and its body's last line to
`return namespace.get(namespace.idFromName(name), options)`. **One `idFromName(`, one
`namespace.get(`, and no `if` on `options`** — measured 2026-09-13, a local `workerd` accepts
`get(id, undefined)` and returns a live stub, so a branch would be a branch guarding nothing.
Record that measurement in `stubFor`'s docblock beside the existing paragraph.

**7. Keep `hosted-identity.test.ts` compiling.** Its `spyNamespace()` object literal is typed
`Parameters<typeof stubFor<string>>[0]`. `jurisdiction` is optional, so the literal needs no
new member; `get` gains an ignored second parameter only if TypeScript complains. Make the
smallest change that typechecks and nothing else in that file.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p packages/cloudflare && npx vitest run --project node packages/cloudflare/src/hosted-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'idFromName(' packages/cloudflare/src/hosted-object.ts` counts occurrences of the
      call; filtering comment lines first (`grep -v '^\s*\*' | grep -c 'idFromName('`) yields
      exactly `1`.
    - `grep -v '^\s*\*' packages/cloudflare/src/hosted-object.ts | grep -c 'namespace\.get('`
      yields exactly `1`.
    - `packages/cloudflare/src/hosted-object.ts` contains the literal
      `jurisdiction(HOSTED_JURISDICTION.eu)` exactly once.
    - `packages/cloudflare/src/hosted-object.ts` contains `'fedramp-high'` and a docblock line
      naming the date `2026-09-13` and the word `workerd` within ten lines of it.
    - `packages/cloudflare/src/hosted-object.ts` contains no function whose parameter list
      accepts both a jurisdiction and a location hint — verified by reading: no identifier
      matching `/placement|createRegion/` exists in the file.
    - No function named for `us` placement exists; `euJurisdictionOf`'s docblock contains the
      phrase `different object ID`.
    - `git diff --stat -- packages/cloudflare/src/worker.ts` is empty.
    - `npx tsc --noEmit -p packages/cloudflare` exits 0, with `EXIT=$?` read on the line
      immediately after the command and never after a pipe.
  </acceptance_criteria>
  <done>`hosted-object.ts` declares the four-member union, the one-member jurisdiction constant, the one-member hint set, two named refusals and two separately-named helpers; `stubFor` takes an optional third argument it passes through without branching; the repository still holds exactly one `idFromName` call and one `namespace.get` call; `worker.ts` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the two helpers reach two different platform calls, and refuse what is not declared</name>
  <files>packages/cloudflare/src/hosted-placement.test.ts, vitest.config.ts</files>
  <read_first>
    - `packages/cloudflare/src/hosted-identity.test.ts:185-230` — `spyNamespace()`, the recording
      fixture this spec extends rather than reinvents, and the comment explaining why the
      assertion is on *what was asked for* rather than on the returned stub.
    - `packages/cloudflare/src/hosted-object.ts` — as left by Task 1.
    - `vitest.config.ts:640-700` and `:975-990` — the hand-derived `files` / `tests` /
      `unitFiles` / `unitTests` block, its dated derivation notes, and the recorded lesson that
      the counts are **counted** and never obtained by arithmetic on a stale total.
    - `packages/node/src/slow-specs.node.test.ts` — the identity `unitFiles === files -
      excludedInNode`, which reddens if only one side moves.
  </read_first>
  <action>
`agent-now`. Pure in-process spec; no runtime, no ports, no network.

Create `packages/cloudflare/src/hosted-placement.test.ts` — a bare `*.test.ts`, so it runs in
the `node` lane and the `browser` lane, which is correct: these are pure functions and a spec
that only ran where a platform global exists could not tell a port from a fixture.

Build a recording fixture in the shape of `hosted-identity.test.ts`'s `spyNamespace()`, widened
to record four things: every `name` passed to `idFromName`, every `options` argument passed to
`get`, every `jurisdiction` value the namespace was narrowed with, and whether the returned
namespace was the original object or a distinct one.

Cases, each asserting on the recording and never on a stub string alone:

1. **The `eu` path narrows the namespace before anything is sited.** `stubFor(euJurisdictionOf(spy), 'bootstrap-eu')`
   records exactly one jurisdiction narrowing with the value `'eu'`, records `idFromName` called
   once with `'bootstrap-eu'` **on the narrowed namespace and not on the original**, and records
   `get` called with `options === undefined`. Assert the narrowing was recorded **before** the
   `idFromName` — order, not merely presence, because a narrowing applied afterwards sites the
   object in the wrong place while every other assertion still passes.
2. **The `sam` path sites on the plain namespace and carries a hint.**
   `stubFor(spy, 'bootstrap-sam', samLocationHint())` records **zero** jurisdiction narrowings,
   `idFromName` once with `'bootstrap-sam'`, and `get` with `{ locationHint: 'sam' }`.
3. **The `us` path carries neither.** `stubFor(spy, 'bootstrap-us')` records zero narrowings and
   `get` with `options === undefined`. Written as its own case so the three readings sit side by
   side and a reader sees three different shapes rather than one parameterised loop. **Do not
   write these three as a table-driven loop** — a loop over a placement descriptor is the shape
   `HOST-06` names as the failure, reproduced in the spec.
4. **A namespace with no `jurisdiction` method is refused by name.** `euJurisdictionOf({})`
   throws `UnsupportedJurisdictionError` and the message contains `'eu'`.
5. **The hint set is closed.** Assert `HOSTED_LOCATION_HINTS` has length `1` written as the
   literal `1` — never `Object.keys(...).length`, because an assertion that reuses the value it
   tests moves with it.
6. **Anti-vacuity floor.** Assert `HOSTED_OBJECT_NAMES.length` is the literal `3` and that the
   spy recorded a non-zero number of `idFromName` calls across the file, so a fixture that
   silently stopped recording fails here rather than passing everything above.

Then update `vitest.config.ts`: re-derive `files`, `tests`, `unitFiles` and `unitTests` by
**counting** a real run, not by adding one to the stored figure, and add a dated note in the
existing style naming `packages/cloudflare/src/hosted-placement.test.ts` as the arriving file
and which lanes it runs in.
  </action>
  <verify>
    <automated>npx vitest run --project node packages/cloudflare/src/hosted-placement.test.ts && npx vitest run --project node packages/node/src/slow-specs.node.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project node packages/cloudflare/src/hosted-placement.test.ts` exits 0
      with at least 6 tests reported; `EXIT=$?` read on the line immediately after.
    - The spec contains the literal `'eu'` and the literal `'sam'` and asserts ordering with a
      comparison of two recorded indices, not merely `toContain`.
    - The spec contains no `for` / `forEach` / `it.each` over the three regions.
    - **Plant, watched red, restored by the surgical inverse:** snapshot
      `packages/cloudflare/src/hosted-object.ts` with `cp` to a path outside the repository
      immediately before editing; change `euJurisdictionOf` to return `namespace` unchanged;
      re-run the spec and record the observed failure text in the SUMMARY; reverse exactly that
      line; verify with `cmp` against the snapshot. If the plant leaves the spec green, report a
      **blind instrument** in the SUMMARY and name which case was supposed to carry the claim —
      do not accept it as redundancy.
    - `npx vitest run --project node packages/node/src/slow-specs.node.test.ts` exits 0, proving
      `unitFiles === files - excludedInNode` still holds after the count update.
    - `vitest.config.ts` contains a new dated note naming `hosted-placement.test.ts`.
  </acceptance_criteria>
  <done>A spec reads the two placements as two different recorded platform interactions with the narrowing ordered before the siting, refuses an undeclared value by name, carries a literal anti-vacuity floor, and a watched plant on `euJurisdictionOf` was seen red and restored `cmp`-clean. The vitest counts are re-derived by counting and the slow-specs identity holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deployment config → object siting | a placement value decides, irreversibly, which object a name resolves to |
| source edit → the already-created object | the `us` object exists and has carried real traffic; an edit to its path re-addresses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `stubFor` / `euJurisdictionOf` | mitigate | A jurisdiction applied to the `us` path derives a different object ID and orphans the live identity permanently. Task 1 leaves `worker.ts` byte-unchanged (acceptance criterion: empty `git diff --stat`), plan 33-02 pins the line in the deploy guard, and `euJurisdictionOf`'s docblock states the mechanism so a later reader does not "tidy" the three paths into symmetry |
| T-33-02 | Elevation of privilege | `HOSTED_LOCATION_HINT`, `HOSTED_JURISDICTION` | mitigate | The platform validates neither (measured 2026-09-13: `locationHint: 'notareal'` accepted). Closed `as const` sets plus `UnknownLocationHintError` / `UnsupportedJurisdictionError` are the only refusal in existence; a widened set is a source edit and a review, which is the point |
| T-33-03 | Spoofing | placement helpers | accept | Neither helper takes a parameter reachable from a request: `euJurisdictionOf` takes a namespace binding and `samLocationHint` takes nothing. The request-derived-name threat is `HOST-12`'s and is already guarded at `hosted-tier-deploy.node.test.ts:238` |
| T-33-04 | Repudiation | the three measurements in `<context>` | mitigate | Readings that live only in a session transcript get re-derived wrongly or assumed backwards. Each is written into a docblock with its date and its source (the wrangler schema file, the workerd binary's own type text, a local `workerd` run) by Task 1's action |
</threat_model>

<verification>
- `npx tsc --noEmit -p packages/cloudflare` exits 0.
- `npx vitest run --project node packages/cloudflare/src/hosted-placement.test.ts packages/cloudflare/src/hosted-identity.test.ts` exits 0.
- `npx vitest run --project node packages/node/src/hosted-tier-deploy.node.test.ts` exits 0 **unchanged** — this plan must not redden the existing `HOST-08` guard, and that is the check that says the `us` path did not move.
- `git status --porcelain` names only this plan's files. Read `EXIT=$?` on the line immediately
  after each command; zsh has no `PIPESTATUS`, it is `pipestatus[1]`.
</verification>

<success_criteria>
- Two placement mechanisms are expressed by two separately-named functions carrying their own
  literals, with no shared placement parameter and no internal branch on one.
- The repository still holds exactly one `idFromName` call and one `namespace.get` call.
- `worker.ts` and the deployed `us` path are byte-unchanged.
- A plant on `euJurisdictionOf` was watched red and restored `cmp`-clean, with the observed
  failure text recorded.
</success_criteria>

<output>
After completion, create
`.planning/phases/33-three-regions-and-a-relay-killed-on-purpose/33-01-SUMMARY.md`.

Commit with explicit paths — `git commit -m "…" -- <path> <path>` — never bare: concurrent
agents share one index. Read `git show --stat` afterwards and confirm only this plan's files
are in it.

**The SUMMARY is bound by criterion 2 the same way this plan is.** Region names are addresses.
Do not write a city or a country in it, including as an example.
</output>
