---
phase: 33-three-regions-and-a-relay-killed-on-purpose
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - packages/cloudflare/src/jurisdiction-closed.node.test.ts
  - packages/cloudflare/src/placement-runtime.e2e.test.ts
  - vitest.config.ts
autonomous: true
requirements: [HOST-06]

must_haves:
  truths:
    - "Passing the hint value where a jurisdiction is required is watched failing, by name, with the permitted values in the failure text"
    - "The same arrangement compiles when the permitted value is passed instead, in the same run"
    - "The tree records that a local runtime cannot tell the two apart, so nobody later builds a local arm that proves nothing"
  artifacts:
    - path: "packages/cloudflare/src/jurisdiction-closed.node.test.ts"
      provides: "the compile-time refusal and its positive control, both taken in one run"
    - path: "packages/cloudflare/src/placement-runtime.e2e.test.ts"
      provides: "the recorded reading that a local runtime refuses every jurisdiction value identically and validates no hint at all"
  key_links:
    - from: "packages/cloudflare/src/jurisdiction-closed.node.test.ts"
      to: "packages/cloudflare/src/hosted-object.ts"
      via: "a scratch module importing DurableObjectJurisdiction and compiled by a real tsc"
      pattern: "DurableObjectJurisdiction"
---

<objective>
Criterion 1's negative proof: *"a plan that passes `sam` as a jurisdiction is watched failing at
creation"* (`.planning/ROADMAP.md:2424`).

**This plan's operator-visible slice:** after it, an operator can run one spec and watch the
refusal happen, and can read in the tree why the refusal cannot be taken where the criterion's
wording implies it would be.

**Both tasks are `agent-now`.** The live at-creation failure — the same value refused by
Cloudflare's own API at the moment an object is made — is `waits on owner act 2`.

## The instrument the criterion names cannot see the property, and this is measured

Measured 2026-09-13 against a throwaway worker on a local `wrangler dev`: `namespace.jurisdiction(v)`
throws `Error: Jurisdiction restrictions are not implemented in workerd.` for **every** `v` —
the permitted values and the hint value alike, byte-identical messages. A local arm that
"watched `sam` fail" would therefore be reporting a property of the runtime and not of the
value: a blind instrument, of exactly the kind this project has recorded catching twelve of in
one run.

So the proof moves to the earliest point at which creation code can exist, which is where the
refusal is real and discriminating: **the platform's own closed type**. The installed workerd
declares `type DurableObjectJurisdiction = "eu" | "fedramp" | "fedramp-high" | "us"`, plan 33-01
declared that union in source, and a module passing the hint value to it does not compile while
a module passing the permitted value does — in the same run, against the same compiler.

This is not a narrowing of the criterion. The criterion's stated instrument is recorded as
unable to see the property, a discriminating instrument is substituted, and the live half is
named as owner-gated rather than quietly dropped.

Purpose: `HOST-06` (`.planning/REQUIREMENTS.md:2193`).
Output: two specs, updated `vitest.config.ts` counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-three-regions-and-a-relay-killed-on-purpose/33-CONTEXT.md
@.planning/phases/33-three-regions-and-a-relay-killed-on-purpose/33-01-SUMMARY.md
@CLAUDE.md

**The shape to copy is Phase 32's `ANNOUNCE_MULTIADDRS` arm** (`.planning/ROADMAP.md:2398-2405`):
it read the refusal **by name from the runtime's own output** — `Uncaught NoAnnouncedAddressError`
— and recorded explicitly that reading only a client-side `[object ErrorEvent]` would have been
attribution by plausibility. Both arms below read a named refusal out of the tool's own output,
never a failed call inferred from the outside.

**tsconfig facts, read 2026-09-13 so the executor does not have to.** The root `tsconfig.json`
sets `allowImportingTsExtensions: true`, `noEmit: true`, `strict: true`,
`verbatimModuleSyntax: true`, `moduleResolution: "bundler"`, `erasableSyntaxOnly: true`.
`packages/cloudflare/tsconfig.json` is `{ extends: "../../tsconfig.json", include:
["src/**/*.ts", "src/**/*.d.ts"] }`. A scratch `tsconfig.json` written into a `mkdtemp`
directory may `extends` the root config **by absolute path** and `include` a single scratch
module that imports `hosted-object.ts` by absolute path with its `.ts` extension.

**Scratch files go under the OS temp directory, never inside the repository.** An untracked
file inside the tree makes `git status --porcelain` dirty, which reddens
`discover-arm.node.test.ts` and `bench-attestation.node.test.ts` for whoever is running them —
this repository shares one working tree between concurrent agents.
`check-copy.node.test.ts:41-46` states this rule and is the precedent.
</context>

<interfaces>
<!-- From packages/cloudflare/src/hosted-object.ts after plan 33-01. -->

  export type DurableObjectJurisdiction = 'eu' | 'fedramp' | 'fedramp-high' | 'us'
  export const HOSTED_JURISDICTION = { eu: 'eu' } as const
  export const HOSTED_LOCATION_HINT = { sam: 'sam' } as const
  export function euJurisdictionOf<Stub>(namespace: HostedObjectNamespace<Stub>): HostedObjectNamespace<Stub>
  export class UnsupportedJurisdictionError extends Error
  export class UnknownLocationHintError extends Error

<!-- The local runtime's own answers, measured 2026-09-13. Assert these as literals. -->

  ns.jurisdiction('eu')          → throws Error: Jurisdiction restrictions are not implemented in workerd.
  ns.jurisdiction('sam')         → throws the SAME message, byte for byte
  ns.get(id)                     → live stub
  ns.get(id, { locationHint: 'sam' })            → live stub
  ns.get(id, { locationHint: <undeclared> })     → live stub, no refusal of any kind
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Watch the hint value refused where a jurisdiction is required, with the permitted value compiling beside it</name>
  <files>packages/cloudflare/src/jurisdiction-closed.node.test.ts, vitest.config.ts</files>
  <read_first>
    - `packages/cloudflare/src/hosted-object.ts` — as left by plan 33-01, for the exported names
      and the union's members.
    - `tsconfig.json` at the repository root — the flags a scratch config inherits.
    - `packages/node/src/check-copy.node.test.ts:41-95` — the scratch-file-under-tmp rule, and
      `spawnSync` with the status read off the result object and never off a pipeline.
    - `vitest.config.ts` — the hand-derived counts block and its dated notes.
  </read_first>
  <action>
`agent-now`. No network, no runtime, no port.

Create `packages/cloudflare/src/jurisdiction-closed.node.test.ts` — `.node.` because it writes
files and spawns a compiler, neither of which exists in the browser lane.

Per case, build a `mkdtemp` directory holding two files:
  - `tsconfig.json` — `{ "extends": "<absolute path to the repo root tsconfig.json>", "include": ["./probe.ts"] }`
  - `probe.ts` — importing `DurableObjectJurisdiction` and `euJurisdictionOf` from the absolute
    path of `packages/cloudflare/src/hosted-object.ts` (with the `.ts` extension), declaring a
    `const value: DurableObjectJurisdiction = '<the value under test>'`, and passing it into a
    local function typed to take a `DurableObjectJurisdiction`.

Run `npx tsc --noEmit --project <the scratch tsconfig>` with `spawnSync`, reading `status` off
the result object. Never build a shell pipeline for this: a pipeline reports the last command's
status and this repository has had a failing run report success that way more than once.

The two arms, **in one run, from one helper differing only in the substituted value**:

1. **The refusal.** The value substituted is `HOSTED_LOCATION_HINT.sam`, read out of the module
   rather than typed into this file. Assert `status` is non-zero **and** that the compiler's
   own stdout+stderr names the value in quotes and names the union's members. Assert the
   presence of each of the four member literals in the diagnostic text, read from the module's
   exported union via a declared array in this spec — so a union member added upstream makes
   this case fail rather than silently pass on three of four.
2. **The control.** The same helper, the same scratch shape, with `HOSTED_JURISDICTION.eu`
   substituted. Assert `status` is exactly `0`. Without this the first arm proves only that
   `tsc` can fail.
3. **The discrimination, asserted rather than implied.** One case that runs both arms and
   asserts the two statuses differ — `expect(refusedStatus).not.toBe(controlStatus)` — with a
   comment stating that this is the whole reading: the compiler distinguishes the two values,
   and the local runtime (see the e2e in Task 2) does not.
4. **Anti-vacuity floor.** Assert the compiler was actually reached: the control run's combined
   output contains no text matching `/error TS5\d{3}/` (a configuration error rather than a
   type error), and the scratch `probe.ts` written for the control is non-empty on disk.
   A `tsc` that never reached the probe would otherwise satisfy arm 2 by exiting 0 over nothing.

Record the refusal's exact first diagnostic line in the SUMMARY, verbatim.

Then update `vitest.config.ts`: re-derive `files`, `tests`, `unitFiles`, `unitTests` by
**counting**, never by adding to a stored total, and add a dated note naming both files arriving
in this plan and the lane each runs in.
  </action>
  <verify>
    <automated>npx vitest run --project node packages/cloudflare/src/jurisdiction-closed.node.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project node packages/cloudflare/src/jurisdiction-closed.node.test.ts`
      exits 0, with `EXIT=$?` read on the line immediately after the command.
    - The spec asserts a non-zero `tsc` status for the hint value and exactly `0` for the
      permitted value, in the same run.
    - The spec contains no location literal of any kind; the value under test is read from
      `HOSTED_LOCATION_HINT` and the control from `HOSTED_JURISDICTION`.
    - The four union members are asserted present in the diagnostic text, sourced from a
      declared array in the spec and not from a substring search of the module's text.
    - **Plant, watched red, restored by the surgical inverse:** snapshot
      `packages/cloudflare/src/hosted-object.ts` with `cp` to a path outside the repository
      immediately before editing; widen `DurableObjectJurisdiction` to `string`; re-run;
      record the observed failure text; reverse exactly that edit; verify with `cmp` against the
      snapshot. If this leaves the spec green, report a **blind instrument** in the SUMMARY.
    - After the run, `git status --porcelain` names no file under the repository that this plan
      did not create — every scratch artifact was under the OS temp directory.
    - `npx vitest run --project node packages/node/src/slow-specs.node.test.ts` exits 0.
  </acceptance_criteria>
  <done>One spec compiles two scratch modules that differ only in a substituted value, watches the hint value refused with the permitted values named in the compiler's own diagnostic, watches the permitted value compile at exit 0 in the same run, asserts the two statuses differ, carries an anti-vacuity floor against a configuration error, and a plant widening the union was watched red and restored `cmp`-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Record that a local runtime refuses every jurisdiction value identically and validates no hint</name>
  <files>packages/cloudflare/src/placement-runtime.e2e.test.ts</files>
  <read_first>
    - `packages/cloudflare/src/admission-slices.e2e.test.ts:70-230` — the `wrangler dev` spawn,
      the `--local-protocol http`, the per-child `mkdtemp --persist-to`, the
      `CLOUDFLARE_API_TOKEN: ''` / `WRANGLER_SEND_METRICS: 'false'` environment, the readiness
      poll, and the `afterAll` teardown. Copy this arrangement; do not invent a second one.
    - `packages/node/src/hermetic-fixtures.node.test.ts` — the rule that a spec must not dial a
      real external endpoint, and the record of a node-lane spec that broke it this week and
      could never redden because it was outcome-stable.
    - `packages/cloudflare/wrangler.jsonc` — the shape a minimal scratch configuration copies.
  </read_first>
  <action>
`agent-now`. Loopback only; no deployed object is touched and nothing is created in the account.

Create `packages/cloudflare/src/placement-runtime.e2e.test.ts`.

**The subject is a throwaway worker, not this repository's worker.** Write into a `mkdtemp`
directory a minimal `worker.ts` and `wrangler.jsonc` declaring one Durable Object class with a
`fetch` returning a fixed string, one `BOOTSTRAP`-style binding, and a `new_sqlite_classes`
migration. The scratch worker's default `fetch` reads two query parameters — a jurisdiction
value and a hint value — and answers JSON reporting, for each, whether the call succeeded and
the `name` and `message` of anything thrown. Adding a route to this repository's own
`worker.ts` for this reading is not allowed: `worker.ts:17-51` states that a route added here is
a capability shipped by a phase, and this is a measurement of the platform, not a capability of
the node.

Boot it with `wrangler dev` on port **8835** — ports 8791-8798, 8801-8810, 8814-8820 and
8822-8826 are taken, and the executor re-verifies with a grep for four-digit ports across
`packages/*/src/*.ts` at write time and names the chosen port and that check in a comment.
Spawn the repository's own `node_modules/.bin/wrangler` **by absolute path** with `cwd` set to
the scratch directory — `npx` from a temp directory will not resolve it. Use `--local-protocol
http`, its own `--persist-to` under the same `mkdtemp`, `CLOUDFLARE_API_TOKEN: ''` and
`WRANGLER_SEND_METRICS: 'false'`. Tear the child down and remove the directory in `afterAll`.

The readings, all in one boot:

1. **The permitted value is refused.** Ask for the permitted jurisdiction value; assert the
   answer carries a thrown error whose message is exactly
   `Jurisdiction restrictions are not implemented in workerd.` — written as a literal in the
   spec.
2. **The hint value is refused with the same message.** Assert the two messages are **equal**,
   and write the assertion as a comparison of the two observed strings rather than two separate
   `toBe(LITERAL)` checks: the reading is that they do not differ.
3. **The conclusion, asserted rather than left in prose.** A case whose title says the local
   runtime cannot distinguish the two, asserting equality and stating in a comment that this is
   why criterion 1's negative proof is taken by the compiler in
   `jurisdiction-closed.node.test.ts` and by Cloudflare's own API during owner act 2, and is
   **not** taken here.
4. **The positive control.** In the same boot, a request that asks for **no** jurisdiction
   returns the object's fixed string. Without it, all three readings above are equally
   satisfied by a worker that fails on everything.
5. **The hint is unvalidated by the platform.** Two requests, one with the declared hint value
   and one with an undeclared string — use `not-a-declared-hint`, which is not a place name —
   both return the object's fixed string with no error. A comment records that this is why
   `HOSTED_LOCATION_HINT` exists: the closed set in source is the only refusal that exists
   anywhere.

Do not update `vitest.config.ts` here — Task 1's count update names both arriving files.
  </action>
  <verify>
    <automated>npx vitest run --project e2e packages/cloudflare/src/placement-runtime.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project e2e packages/cloudflare/src/placement-runtime.e2e.test.ts` exits
      0, with `EXIT=$?` read on the line immediately after; read the `[host conditions]` banner
      before quoting any duration.
    - The spec contains the literal string
      `Jurisdiction restrictions are not implemented in workerd.` exactly once, and the
      permitted-value / hint-value equality is asserted as a comparison of two observed values.
    - The positive control case asserts the object's fixed string, so a worker that failed on
      everything cannot satisfy the file.
    - The undeclared hint arm uses `not-a-declared-hint` and no place name appears anywhere in
      the file.
    - The spec dials `127.0.0.1` only. `grep -E 'https?://(?!127\.0\.0\.1|localhost)' ` over the
      file finds nothing.
    - After the run, `git status --porcelain` names no scratch file — the throwaway worker and
      its persist directory were under the OS temp directory and were removed.
    - **Plant, watched red, restored:** change the asserted refusal literal by one character,
      re-run, record the observed failure text, restore by the surgical inverse and `cmp` against
      a snapshot taken immediately before. This plant is on the spec's own literal rather than on
      shared source, so it cannot collide with another agent.
  </acceptance_criteria>
  <done>One local boot records that the runtime's jurisdiction refusal is value-independent and that no hint value is validated, with a positive control in the same run proving the harness reads a working object, and the tree now says in a spec why no local runtime arm carries criterion 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec → the developer's machine | a spec that spawns a runtime can reach the network or the account if written carelessly |
| a green spec → a closed criterion | an instrument that cannot see the property closes a criterion on nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Tampering | the compile-time arm | mitigate | A `tsc` failing for an unrelated reason (a bad scratch config) satisfies a naive non-zero-exit assertion. Task 1 asserts the diagnostic names the value **and** the union's four members, asserts the control exits exactly 0 in the same run, and carries a floor refusing `TS5xxx` configuration errors |
| T-33-11 | Spoofing | `placement-runtime.e2e.test.ts` | mitigate | A spec that reached a real endpoint would report the network rather than the runtime, and being outcome-stable it could never redden — the failure `hermetic-fixtures.node.test.ts` records from this week. Loopback only, an explicit `127.0.0.1`-only acceptance criterion, `CLOUDFLARE_API_TOKEN: ''` |
| T-33-12 | Denial of service | the scratch `wrangler dev` child | mitigate | An orphaned child holds a port and dirties later runs. `afterAll` kills the child and removes the `mkdtemp` tree; the port is chosen outside the taken set and re-verified by grep at write time |
| T-33-13 | Repudiation | the runtime measurement | mitigate | The 2026-09-13 reading exists only in a session transcript. Task 2 puts it in the tree as an executable assertion, so a workerd release that starts implementing jurisdictions reddens here instead of silently making a docblock false |
| T-33-14 | Information disclosure | the scratch worker | accept | It handles no secret, binds no real namespace and lives under the OS temp directory for the length of one run. Nothing it holds is worth disclosing |
</threat_model>

<verification>
- `npx vitest run --project node packages/cloudflare/src/jurisdiction-closed.node.test.ts` exits 0.
- `npx vitest run --project e2e packages/cloudflare/src/placement-runtime.e2e.test.ts` exits 0.
- `npx vitest run --project node packages/node/src/slow-specs.node.test.ts` exits 0.
- `git status --porcelain` names only this plan's files.
- Run the two lanes separately. `aot` is not involved and is not run here.
</verification>

<success_criteria>
- The hint value is watched failing where a jurisdiction is required, by name, with the
  permitted values in the failure text, and the permitted value compiles in the same run.
- The tree records — as an assertion, not as prose — that a local runtime refuses every
  jurisdiction value identically, so no later agent builds a local arm that proves nothing.
- Both plants were watched red and restored `cmp`-clean, with their observed text recorded.
</success_criteria>

<output>
After completion, create
`.planning/phases/33-three-regions-and-a-relay-killed-on-purpose/33-03-SUMMARY.md`.

State plainly in it: the compile arm is criterion 1's negative proof as taken by an agent; the
live at-creation refusal from Cloudflare's own API is `waits on owner act 2` and is not claimed
here.

Commit with explicit paths — never bare. **The SUMMARY is bound by criterion 2**: no city, no
country, not even as an example.
</output>
